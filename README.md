# API CRUD com Flask + MySQL

Projeto de exemplo com Python, Flask, MySQL e Docker (em breve), com rotas completas de CRUD e versionamento no GitHub.

## Como rodar localmente

```bash
pip install -r requirements.txt
python app.py
